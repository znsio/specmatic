version: 2
contracts:
  - filesystem:
      directory: "."
    consumes:
      - ./product.yaml
      - baseUrl: http://localhost:9001
        specs:
          - ./order.yaml
          - ./exported_product/exported_product.yaml
