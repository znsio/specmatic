contract_repositories:
  - type: filesystem
    test:
      - products.yaml

workflow:
  ids:
    'POST /products -> 201':
      extract: 'BODY.id'
    'PATCH /products/(id:string) -> 200':
      extract: 'BODY.id'
    '*':
      use: 'PATH.id'
